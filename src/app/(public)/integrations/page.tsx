import { IntegrationsContent } from "@/components/integrations/IntegrationsContent";

export const metadata = {
  title: "Tech Stack | Sourcecode",
  description: "We use the best tools for every project. React, Node.js, Python, AWS, WordPress, and custom APIs — the modern stack behind Sourcecode solutions.",
};

export default function IntegrationsPage() {
  return <IntegrationsContent />;
}
